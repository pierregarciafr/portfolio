<section id="portfolio">
      <div class="container">
        <h1>Portfolio</h1>
          <div id="cards_section">
            <%= render 'card' %>
            <%= render 'card' %>
            <%= render 'card' %>
            <%= render 'card' %>
          </div>
      </div>
</section>
